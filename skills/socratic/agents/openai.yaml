interface:
  display_name: "Socratic"
  short_description: "Orchestrate intent discovery and test refutation"
  default_prompt: "Use $socratic to review this change: confirm intent with the specification owner, challenge the tests with risk-directed mutations, and propose missing tests without applying them unless I explicitly ask."

policy:
  allow_implicit_invocation: false
