interface:
  display_name: "Maieutic"
  short_description: "Draw out intent and propose risk-driven unit tests"
  default_prompt: "Use $maieutic to analyze this diff, clarify only decisions that change test expectations, and propose missing unit tests without applying them unless I explicitly ask."
